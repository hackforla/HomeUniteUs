delete:
  description: Delete user from AWS Cognito and DB
  operationId: delete
  responses:
    "200":
      content:
        application/json:
          schema:
            $ref: "../../openapi.yaml#/components/schemas/ApiResponse"
      description: successful operation
  tags:
    - auth
  x-openapi-router-controller: openapi_server.controllers.auth_controller
  security:
    - jwt: ["secret"]
